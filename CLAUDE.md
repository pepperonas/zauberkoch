# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Projekt: Zauberkoch 🧑‍🍳🍸 — KI-Rezept- & Cocktail-Generator

Mobile-first Web-App unter **https://zauberkoch.de**: Nutzer wählt Modus (Kochen/Cocktail), Länderküche, Geschmacksrichtungen und Constraints in einem 3-Schritt-Wizard — die Claude-API generiert ein vollständiges Rezept, das sich **live per SSE-Streaming aufbaut** (Titel → Zutaten → Schritte). Repo: `pepperonas/zauberkoch-pwa` (öffentlich, Open Source — auf der Landing-Page verlinkt). **Offene Selbstbedienungs-Registrierung** via Google; **zur Laufzeit im Admin-Panel umschaltbar** (Toggle „Offene Registrierung", `app_settings.open_signup`, überschreibt den `OPEN_SIGNUP`-Config-Default) — aus → Allowlist gatet. Invite-Codes wurden entfernt (2026-07-12).

## Tech-Stack

| Layer | Technologie |
|---|---|
| Backend | Python 3.12, FastAPI, SQLite (WAL), SQLAlchemy 2, Pydantic v2, Alembic |
| KI | Anthropic API, Modell via env `ANTHROPIC_MODEL` (Default `claude-sonnet-5`), Streaming → semantische SSE-Events |
| Frontend | React 19, Vite, TypeScript strict, TanStack Query |
| Styling | Material 3 Expressive — **handgebaut** (Design-Tokens als CSS Custom Properties, KEIN MUI/Ant) |
| Animation | Motion (framer-motion-Nachfolger), echte Spring-Physik |
| Auth | Google OAuth 2.0 (Auth Code + PKCE) **und E-Mail/Passwort** (scrypt, Double-Opt-in), Sessions als httpOnly-Cookies, CSRF-Schutz |
| Deployment | VPS 69.62.121.168: systemd `zauberkoch-api` (Port **8742** loopback) + statischer Build in `/var/www/zauberkoch.de/`, vHost im zentralen nginx, certbot. **KEIN Docker.** |

## Struktur

```
backend/          # FastAPI-App (eigenes venv: backend/.venv)
  app/api/v1/     # Router (auth, recipes, favorites, shopping, share, me, health)
  app/core/       # config (pydantic-settings), security, logging
  app/models/     # SQLAlchemy: users, sessions, recipes, favorites,
                  #   shopping_list_items, generation_cache, allowlist, rate_limits
  app/schemas/    # Pydantic (u.a. Recipe — das Rezept-JSON-Schema)
  app/services/   # ai (Anthropic-Streaming + inkrementeller JSON-Parser),
                  #   cache, ratelimit, aggregation (Einkaufsliste)
  app/prompts/    # Rezept-System-Prompts, VERSIONIERT (recipe_v1.py, …) — Kernstück!
  alembic/        # Migrationen (von Anfang an; nie Schema ohne Migration ändern)
  scripts/        # Admin-CLIs: allowlist, stats, smoke_ai, showcase
  tests/          # pytest (Temp-DB, keine echten API-Calls)
frontend/         # React-App
  src/styles/     # tokens.css (M3-Schemata: Basilikum-Grün=Kochen, Violett=Cocktail, je Light+Dark)
  src/i18n/       # de.ts + t() — ALLE UI-Strings hier, nie hartcodiert in Komponenten
  src/motion/     # Spring-Presets/Transitions
  src/features/   # wizard, recipe, cook-mode, favorites, shopping, auth, landing
deploy/           # zauberkoch-api.service, nginx-vhost.conf, deploy.sh, backup-timer
docs/             # DEPLOY.md (Erst-Einrichtung), GOOGLE-OAUTH.md
```

## Befehle

```bash
# Backend (aus backend/)
source .venv/bin/activate
uvicorn app.main:app --reload --port 8742     # Dev-Server
alembic upgrade head                          # Migrationen
pytest                                        # Tests — Pflicht vor Deploy
pytest --cov=app                              # Coverage-Report (pytest-cov)
python -m scripts.allowlist add <email>       # Allowlist verwalten
python -m scripts.stats [tage]                # Usage/Kosten/Cache-Quote/Feedback-Report
python -m scripts.smoke_ai [cocktail]         # 1 echte Generierung (Prompt/Parser-Smoke, kostet Tokens)
python -m scripts.showcase                    # Landing-Galerie seeden (idempotent, auf dem VPS)

# Frontend (aus frontend/)
npm run dev          # Vite-Dev-Server (Proxy /api → localhost:8742)
npm run build        # Production-Build
npm test             # Vitest — Pflicht vor Deploy
npx playwright test  # E2E-Smoke (lokal)
npm run gen:assets   # Brand-Assets (Favicon/PWA/OG) aus dem Chef-Hut-Logo
npm run export:motifs   # Karten-Motive → PNGs für den OG-Renderer (nach RecipeMotif-Änderungen)
npm run measure:motifs  # QA: Bounding-Box je Motiv (Ausreißer = zu klein/groß)

# Deploy (vom Mac)
./deploy/deploy.sh              # Tests → Build → rsync → restart → healthcheck
```

## Konventionen (verbindlich)

- **UI-Texte Deutsch**, Code/Kommentare/Commits Englisch. Strings NUR über `src/i18n/de.ts` (i18n-ready).
- **M3 Expressive handgebaut**: alle Farben/Shapes/Typo über Tokens in `tokens.css`. Moduswechsel Kochen↔Cocktail = Token-Morph (animiert), kein Neu-Rendern.
- **Motion**: echte Springs via Motion-Library — **keine linearen `ease-in-out`-CSS-Transitions als Animations-Ersatz**, keine Inline-Magic-Numbers. **MD3-Expressive-Spring-Tokens in `src/motion/tokens.ts`** (`fastSpatial`/`defaultSpatial`/`slowSpatial` mit `bounce`-Overshoot für spatial; `effectsFast`/`effectsDefault` ohne Overshoot für opacity/color; `staggerIn`, benannte Variant-Presets) — spatial darf überschwingen, Fades nie. `src/motion/springs.ts` (stiffness/damping) bleibt als älterer Satz gültig. Nur `transform`/`opacity` animieren, `will-change` gezielt. `prefers-reduced-motion` → schnelle Fades + kein Partikel-Burst, Pflicht auf jeder Animation. Aufgewertete Signatur-Animationen: **Favorit-Reward** (`FavoriteButton.tsx`), **Hero-Moment** beim Fertigwerden (`GeneratePage`: Settle-Pop + Reveal-Sweep + Haptik + Stagger), **Conjure-Stage** 320px + Atmung + rotierende Wartetexte. **Route-/View-Transitions — 2026-07-15 auf react-routers DATA-ROUTER migriert (authoritative Doku: `.claude/rules/motion.md`):** `createBrowserRouter`+`<RouterProvider>` (`App.tsx`/`main.tsx`), JEDE Navigation trägt `viewTransition` (`<Link/NavLink viewTransition>`, `navigate(to,{viewTransition:true})`), Browser-**und** In-App-„← Zurück" = `navigate(-1)`. react-router besitzt die History und rendert die neue Route SYNCHRON im VT-Callback (`startViewTransition(()=>flushSync(render))`) für PUSH **und POP** → **Browser-Back morpht endlich auf echtem Mobile-Chrome** (verifiziert fine+coarse). Shared-Naming via `useViewTransitionState('/rezept/id')` (statt `activeId`), Scroll via `<ScrollRestoration/>`. Kosten: Entry-Chunk +~15 kB gz (Data-Router-Runtime). **Der frühere manuelle `ViewTransitionProvider`/`go()`/`window.navigation`-Listener ist ENTFERNT** (er konnte den externen Browser-Back-Traverse nicht mit dem VT-Snapshot synchronisieren). ⚠️ **Die folgenden Absätze beschreiben teils das ALTE System und sind überholt, wo sie `go()`/`activeId`/`ViewTransitionProvider`/`intercept()` nennen** — gültig bleiben nur die CSS-Namen (`zk-shared-*`, `zk-d-*`, `zk-nav/header`-Hold, Root-Crossfade) und die Morph-Killer (Suspense-Prewarm, Query-Cache-vor-Navigation, framer-Entrance-Suppression bei aktivem Morph): Navigationen laufen über `useViewTx().go(to, {sharedId})`: RecipeCard-Klick = **PUSH** (`go('/rezept/id', {sharedId:id})`, startet die VT direkt), Bottom-Nav-Tabs (`go(to)` PUSH, ohne sharedId → nur Root-Crossfade). **Back/Forward:** die In-App-„← Zurück" (`go(-1, {sharedId})`) treibt ihren VT **selbst in `go()`** (robust, unabhängig von der Navigation API — v46 machte sie fälschlich Listener-abhängig → beide Back-Wege brachen). Der **Browser-/Maus-Zurück-Button** wird von einem **`window.navigation`-`navigate`-Listener** (Navigation API, `navigationType==='traverse'`) getrieben — `popstate` (auch capture-phase) feuert ERST NACHDEM react-router re-rendert hat (verlassene Seite/Detail-Hero weg → nichts zu benennen), das `navigate`-Event feuert VOR dem Commit (alte DOM noch da). **Browser-Back = observe-only, Hero IMMER benennen, NIE `intercept()` (gefixt 2026-07-15 via User-`?vtdebug=1`-Logs).** Der Bug war ein `fine &&`-Gate beim Hero-Benennen: echte Nutzer sind **coarse** (`fine:false`) → Hero unbenannt → keine Shared-Elemente → harter Cut, während der In-App-`go(-1)` (benennt IMMER) morphte. Fix: `if (sid!=null) flushSync(setActiveId(sid))` **ohne** Pointer-Gate + observe (kein `intercept()` — das unterdrückt `popstate` → old===new → Cut). Verifiziert: morpht auf fine **und coarse** (Pixel-5). **NIE wieder einbauen:** `fine`-Gate, `intercept()` auf Touch (killt Morph — Predictive-Back-Animation ist der kleinere Preis), „INTERCEPT+REDRIVE" (`navigate(replace)` im Handler wirft auf echtem Chrome + `forcedScroll`-Leak zerschoss den In-App-Back). **⚠️ „Title-Bar flackert auf Mobile":** die **App-Kopfzeile** (`.shell__header`, `zk-header`) sprang beim Zurücknavigieren, weil sie `position: sticky` war und der VT-Snapshot-Containing-Block auf Mobile den URL-Bar-Bereich mit abdeckt (Chrome toggelt die Adressleiste zu VT-Beginn → Sticky-Header re-ankert → gehaltener Snapshot springt). **Gefixt 2026-07-15: Header ist `position: fixed`** (viewport-fest, im Snapshot-Block stabil), `.shell` reserviert `padding-top: var(--header-h)`; NIE zurück auf `sticky` (Details `.claude/rules/motion.md`). Die **Browser-Adressleiste selbst** bleibt Browser-Chrome → nur via PWA-Install ganz weg. „VT auf Touch abschalten" killt den Morph → verboten. **Doppel-VT-Guard:** `go(-1)` setzt `skipTraverse`, damit der Listener den von ihm ausgelösten Traverse ignoriert. Fallback ohne Navigation API (Firefox/älteres Safari): In-App-Button morpht weiter, nur der Browser-Button ist un-animiert. **Shared-Element-Container-Transform:** Gericht-**Grafik** (`RecipeMotif style={{viewTransitionName}}`) + **Titel** morphen Position/Größe zwischen Karte und Detail-Hero. Feste Namen `zk-shared-motif`/`zk-shared-title` (`viewTransition.tsx`), gesetzt **nur** am aktiven Element (`activeId === recipe.id`) → pro Snapshot eindeutig, löst automatisch den „viele Items"-Perf-Punkt (nur die geklickte Karte wird benannt). CSS in `base.css`: Theme-Reveal-Regeln auf `.zk-theme-vt` gescopet (sonst würgen sie den Route-Root-Crossfade ab); `::view-transition-group(zk-shared-*)` morpht mit MD3-Emphasized `cubic-bezier(.2,0,0,1)` 360 ms; Root-Content crossfadet (`zk-vt-fade-in/out`, 320/180 ms). **Kritische Mechanik (mühsam erarbeitet):** Der VT-Callback gibt für **beide** Richtungen eine Promise zurück, die erst beim tatsächlichen React-Route-Commit resolved — über einen `pendingResolve`-Ref, den der Scroll-`useLayoutEffect` (auf `location.key`) auslöst (+ 500 ms Safety-Timeout). **KEIN `flushSync(navigate)`**: react-router v7 committet die Navigation nicht synchron (deferred), und rAF ist während der VT-Callback-Phase ausgehungert → beide Wege scheiterten (New-Snapshot = alte Seite → später Swap → Morph gecancelt). Drei weitere Fallen, die den Morph killten: (a) **Suspense** — die Ziel-Seite MUSS beim Commit synchron mit Hero da sein; eine lazy `RecipeDetailPage` suspendet einen Frame → Fallback-Snapshot ohne Hero → Cancel. Lösung: **`RecipeDetailPage` wird per verstecktem Off-Route-Mount vorgewärmt** (`{me && !'/rezept/' && <div hidden><Suspense><RecipeDetailPage/></Suspense></div>}` in App.tsx) → `React.lazy` ist vor der Navigation resolved, Route rendert synchron — OHNE die Seite (RecipeView/RecipeMotif, ~12 KB gz) in den Entry-Chunk zu ziehen (Lighthouse!). Schwere Dialoge (CookMode/ShareDialog) in RecipeDetailPage sind zusätzlich `lazy`. (b) **Query** — `RecipeCard` `ensureQueryData` VOR der Navigation (sonst Ladezustand ohne Hero), `onPointerDown`-Prefetch macht das Warten ~0. (c) **framer-Entrances** — der `riseIn` der `motion.section.hero` wird bei `isShared` unterdrückt (animiert den Motiv-Container und würgte die VT ab). **Ebenso `RecipeCard`**: die gestaffelte `riseIn`-Entrance wird unterdrückt, solange **irgendein** Morph läuft (`activeId != null`) — sonst re-mountet die Liste beim Zurück, die Karten stehen im New-Snapshot noch auf `opacity 0`/y-Offset → Morph-Ziel unsichtbar, Liste wirkt „hart neu gerendert" (genau das Browser-Back-Symptom). Fix 2026-07-14. **Perf:** kein `backdrop-filter`-Jank mehr (VT snapshottet den Header einmalig — der `.zk-page-vt`-Blur-Drop entfiel). **Scroll:** `history.scrollRestoration='manual'`; Position wird in `go()` synchron VOR der Navigation gemerkt (ein Scroll-Listener würde mit dem Vorwärts-`scrollTo(0,0)` racen und 0 speichern), Restore/Reset im `useLayoutEffect` auf `location.key` (POP → gemerkt, sonst 0). **Fallback:** `prefers-reduced-motion` oder fehlendes `startViewTransition` → normale Navigation ohne VT. Der frühere framer-`AnimatePresence`-Route-Wrapper + der tote `layoutId`-Container-Transform wurden entfernt.
- **API-Contract**: anonymes `GET /me` antwortet `200 {authenticated:false}` (nie 401 — Konsolenfehler/Lighthouse). 
- **Security**: `ANTHROPIC_API_KEY` nur server-seitig. Auth-Tokens NIE in localStorage (httpOnly-Cookies). CSRF-Schutz auf state-changing Requests. Scoring/Limits/Validierung server-seitig. CORS strikt auf zauberkoch.de.
- **Rate-Limits (2026-07-14, DB-backed & im Panel editierbar)**: Systemweite Limits leben in der Singleton-Tabelle **`app_settings`** (id=1, Migration `e5f1c2a7b304`) — **fehlt die Row, gilt der Config-Default** (`app/services/limits.py` `get_limits(db)` liest Row **sonst** Config; so bleiben `monkeypatch`-Tests gültig, die Row ist reines Admin-Override, angelegt beim ersten `PATCH`). Felder + Config-Defaults: `default_user_limit` (5), `global_daily_limit` (200), `registration_daily_limit` (20), `anon_ip_limit` (3, Live-Probier pro IP), `anon_global_limit` (15, Anon-Tagesbudget). **Pro-Nutzer:** `User.daily_limit` (nullable) ist maßgeblich; **NULL → `default_user_limit`** (einheitlich, keine getrennte „Neue Konten"-Zahl mehr). **Neue Konten werden mit `daily_limit=NULL` angelegt** (nutzen also den Default) — `default_new_user_limit` wurde entfernt. **Registrierungs-Cap:** `ratelimit.consume_registration(db)` (RateLimit-Scope `registrations`, UTC-Tag) im Google-Callback VOR der User-Anlage; bei Überschreitung `fail("registration_full")` (Redirect), **Bestandslogin unberührt**. `ratelimit.effective_limit`/`consume_generation`/`consume_anon` + der `/recipes/try`-IP-Check lesen alle aus `get_limits`. **Admin-API:** `GET/PATCH /admin/limits` (`_limits_payload` inkl. `registrations_today`; PATCH validiert 0–N, partial). Frontend: editierbare `LimitRow`-Felder (`AdminPage.tsx`, `admin.css` `.admin__limit*`) statt read-only KPIs. **VPS-`.env`:** `DAILY_LIMIT_PER_USER`/`DAILY_LIMIT_GLOBAL` entfernt, damit die Config-Defaults (5/200) greifen bis der Admin im Panel überschreibt. **Admin-Panel** (`AdminPage.tsx`, restrukturiert 2026-07-12): eine Seite mit **Sticky-Sub-Nav** (IntersectionObserver-Active-State, Anker-Scroll, `top` = gemessene Header-Höhe) + 5 Sektionen — **Übersicht** (5 KPI-Cards + Trend-Sparklines für Generierungen/Kosten), **Nutzung** (pro-Nutzer Sparkline + Perioden-/Heute-Zahlen), **Nutzer & Limits** (`GET /admin/users` + `PATCH /admin/users/{id}` 0–1000|null, Stepper), **Feedback** (pro Prompt-Version, skaliert für v5+), **System** (globale Limits + **Registrierungs-Toggle** + Allowlist). **`open_signup` (2026-07-14):** dieselbe `app_settings`-Row + `get_limits`-Machinerie trägt jetzt auch das Bool `open_signup` (Migration `a9c3f1e6b722`, `server_default '1'`); es steckt im `_FIELDS`-Mapping (Column↔Config-Attr, beide `open_signup`), der Google-Callback liest `get_limits(db).open_signup` statt `settings.open_signup` (Row überschreibt Config, Fallback = Config-Default). `GET/PATCH /admin/limits` führen es mit (`LimitsBody.open_signup: bool|None`); Frontend = `Switch` in der System-Sektion, Allowlist-Hinweis wird je nach Zustand dynamisch (`allowlistNoteOpen`/`Closed`). Der `OPEN_SIGNUP`-Env auf dem VPS ist damit nur noch der Vor-Edit-Default. Sparklines = `components/admin/Sparkline.tsx` (Inline-SVG, keine Dependency). Stats-Endpoint additiv um `daily:[{day,gens,cost_usd}]` + pro-`per_user` `series:number[]` erweitert (aus den geladenen Rows gebucketet, keine Extra-Query). CSS `pages/admin.css`. **Test-Conftest** pinnt großzügige Config-Defaults (`daily_limit_per_user=20` u. a.), damit Multi-Gen-Tests Luft haben; Cap-Tests `monkeypatch`en den jeweiligen Config-Wert selbst (greift, weil ohne Row `get_limits` die Config liest).
- **Soft-Delete (2026-07-14)**: Nutzer können Rezepte in der Detailansicht löschen (`danger`-Text-Button unten, abgesetzt via `.detail__danger`-Hairline, Confirm-`Dialog`). `DELETE /recipes/{id}` (CSRF) setzt `recipes.deleted_at` (Alembic `f7b2c9d4e810`, nullable) — **Hard-Delete gibt es nicht**: Row + `generation_cache`-Eintrag bleiben, damit die KI-Ausgabe weiter **gratis aus dem Cache** bedient werden kann. **Alle nutzer-sichtbaren Queries filtern `deleted_at IS NULL`** — zentral über `owned_recipe(db, id, user_id)` in `recipes.py` (+ die lokalen `_owned_recipe`/`_by_token` in favorites/share, die `db.get`-Checks in plan/shopping, und die Joins in plan get_week/week_to_shopping, share `_public_recipes`). **Kosten-Verdrahtung (wichtig):** die Dedup-Queries (`_known_titles`, `seen`) filtern soft-gelöschte AUS → re-generiert der Nutzer dieselben Parameter, greift der **Cache** (gratis) statt des teuren „hat-das-schon → regenerate"-Pfads; `_persist_recipe` **reaktiviert** dabei die gelöschte Row (`deleted_at=None`, kein Duplikat). Deleted Share-Links lösen 404. Admin-Feedback-Aggregat (`admin.py`) bleibt bewusst ungefiltert (interne Prompt-Qualitäts-Analytik). Tests: `tests/test_soft_delete.py`.
- **Cache-Semantik (2026-07-12)**: Der Generation-Cache bedient nur **Erst-Anfragen** (andere Nutzer, Fehler-Retry). Wiederholt derselbe Nutzer identische Parameter, hat er das Rezept schon → Server schaltet automatisch auf `regenerate` und liefert ein **neues** Rezept. Zwei Verfeinerungen: (a) **`personen` ist aus dem Cache-Key ausgenommen** — nur-Personen-Änderung wird gratis aus dem Cache bedient und server-seitig skaliert (`_scale_recipe`); (b) Variationen bekommen die **Avoid-Liste** der bereits erhaltenen Titel (`vermeiden_titel`, server-injected, Client-Werte werden verworfen).
- **Duplikat-Vermeidung (2026-07-22, Prompt v5)**: Zwei *leicht unterschiedliche* Anfragen (z. B. Geschmack „sauer, frisch" vs. „zitrus, erfrischend") lieferten denselben Daiquiri — der Modell-Zug zu berühmten Gerichten ignoriert unsere Parameter-Nuancen. Vier Änderungen, gemeinsam: **(1)** Die Avoid-Liste wird jetzt aus der **Modus-Historie** des Nutzers gespeist (`Recipe.mode == modus`, jüngste `MAX_AVOID_TITLES`=40), NICHT mehr nur aus dem exakten `params_json` — so sehen einander auch abweichende Parameter-Sets. **(2)** Sie wird bei **jeder** Generierung angehängt, nicht nur bei `regenerate` (`recipe_v5.build_user_prompt`: Liste rendert immer, wenn `vermeiden_titel` gefüllt; System-Block identisch zu v3 → Anthropic-Prompt-Cache trifft weiter). **(3)** Cache-Treffer werden über eine **wortstellungs-/schreibweisen-invariante Titel-Identität** geprüft (`app/services/titles.py::title_key`: ü→ue + Diakritika strippen, Füllwörter/Kategorien wie „klassisch"/„Cocktail" raus, Tokens sortiert → „Der klassische Daiquiri" == „Daiquiri"), damit ein umformulierter Cache-Eintrag als Wiederholung erkannt wird und regeneriert. **Bewusst konservativ:** echte Modifikatoren bleiben distinkt (`Erdbeer-Daiquiri` ≠ `Daiquiri`, `vegan`/`grün` sind KEIN Füllwort). **(4)** `vermeiden_titel`-Schema-Cap 10 → 40 (sonst schnitt Pydantic die Liste ab; in Sync mit `MAX_AVOID_TITLES`). **Kostenneutral:** ein Gericht, das der Nutzer noch nie hatte, wird weiter gratis aus dem Cache bedient (Avoid-Liste nur bei Live-Generierung angehängt, leer für historienlose Nutzer). Prompt-Bump = alte v4-Cache-Einträge altern natürlich aus. Tests: `tests/test_titles.py` (Titel-Identität, rein), `tests/test_dedup.py` (Endpoint: Avoid-Liste spannt Parameter-Varianten, Modus-Scope, Titel-Key-Cache-Dedup, Kosten-Guard).
- **Modellwahl geprüft (2026-07-11)**: `claude-sonnet-5` ist der Sweet Spot (~3–4 ct/Rezept live; Haiku spart nur ~2 ct bei sichtbarem Qualitätsverlust, Opus per `ANTHROPIC_MODEL`-Env A/B-testbar, falls Feedback-Daten es nahelegen). **Kostenrechnung (2026-07-26, `app/services/pricing.py`)**: Preise liegen NICHT mehr als Konstanten in `admin.py`/`scripts/stats.py`, sondern in einem Preisplan **je Modell und je Stichtag** — jede Generierung wird mit dem Preis bewertet, der an ihrem Tag galt. Zwei Gründe: (a) Sonnet-5 kostet bis **2026-08-31** $2/$10 und ab **2026-09-01** $3/$15, die Dashboard-Fenster (30/90 Tage) laufen also über den Stichtag — eine feste Zahl wäre auf einer Seite immer falsch (die frühere Lösung war ein manuelles TODO); (b) `generations.model` hält fest, was wirklich lief, und Opus ist per `ANTHROPIC_MODEL` A/B-testbar — zu Sonnet-Preisen gerechnet wäre eine Opus-Zeile um das 2,5-fache zu billig. Modell-Match per **längstem Präfix** (`claude-sonnet-5-20260514` → sonnet-5), unbekanntes Modell → Sonnet-Standard + `is_priced()`-Flag (`stats.py` weist es aus). Cache-Multiplikatoren 0,1× (read) / 1,25× (5-Min-Write) — **gilt nur, solange `ai.py` `cache_control: {"type": "ephemeral"}` ohne `ttl` sendet**; bei `"ttl": "1h"` wird der Write-Multiplikator 2,0. Quelle: platform.claude.com/docs → Pricing (geprüft 2026-07-26). Tests: `tests/test_pricing.py` (16: Stichtags-Grenze 31.08./01.09., ein Fenster über beide Sätze, Modell-Lookup inkl. sonnet-4 vs. sonnet-5, Cache-Faktoren, naive Timestamps = UTC, Offset-Umrechnung, Fallback ohne passende Periode).
- **Transaktions-Mail (2026-07-12)**: `app/services/mailer.py` — Jinja2-Render (`app/templates/email/<name>.{html,txt}.j2`, HTML autoescaped, Text literal) + `smtplib` **multipart/alternative** (SMTP_SSL:465 oder STARTTLS). Env `SMTP_HOST/PORT/USER/PASS/FROM/FROM_NAME`; **leerer `SMTP_HOST` = No-Op + Warnung** (dev/tests fassen nie das Netz an). Prod nutzt den **VPS-Hostinger-Mailer** (`support@celox.io`, Anzeigename „Zauberkoch"; Creds aus `/opt/sicherheits-check-api/.env` wiederverwendbar). **Verify-Mail-Template** = dunkles Marken-Theme, bulletproof (Tabellen, Inline-Styles, VML-Button für Outlook, `color-scheme`-Meta + `[data-ogsc]`-Overrides gegen Gmail-Invertierung, Preheader, Alt-Texte, Impressum-Footer). Preview für Litmus/Mail-Tester: `python -m scripts.email_preview` → `email-preview.html` (gitignored). **Templates teilen sich `_layout.html.j2` + `_macros.html.j2`** (DRY: Scaffold + bulletproof CTA/Fallback/Note-Makros); vorhanden: `verify`, `reset`, `welcome`, `admin_signup`. Mailer-Funktionen: `send_verification_email`/`send_reset_email`/`send_welcome_email`/`send_admin_signup_notification`. Versand läuft über FastAPI-`BackgroundTasks` (schnelle Antwort).
- **E-Mail/Passwort-Auth (2026-07-26)**: klassische Anmeldung **zusätzlich** zu Google, ohne den Google-Flow zu ändern. Endpunkte in `app/api/v1/auth.py`: `POST /register` (Double-Opt-in), `/verify` (Bestätigung + Auto-Login), `/login`, `/forgot`, `/reset`. **Passwörter**: stdlib `hashlib.scrypt` mit Zufalls-Salt, selbstbeschreibendes Format `scrypt$n$r$p$salt$hash` (`app/services/passwords.py`, dependency-frei; Timing-safe, hasht immer — auch bei fehlendem Konto). **Tokens zustandslos** (`app/services/auth_tokens.py`) über den bestehenden HMAC-`sign_payload` — KEINE Token-Tabelle: Verify 24 h + idempotent, Reset 1 h + an einen Passwort-Hash-Fingerprint gebunden (⇒ Einmal-Nutzung, alte Links sterben bei Passwortänderung). Reset invalidiert alle Sessions des Kontos. **Migration** `a1b2c3d4e5f6`: `users.password_hash`/`email_verified_at` (nullable) + `google_sub` → nullable (E-Mail-Nutzer haben keine google_sub; SQLite erlaubt mehrere NULLs im Unique-Index). **DB-Modell:** Google-Konten werden `email_verified_at`-gesetzt angelegt; E-Mail-Konten bleiben inaktiv (Login 403 `email_unverified`) bis bestätigt. **Sicherheit**: Enumeration-safe (Register/Forgot geben immer dieselbe generische Antwort; Login immer `invalid_credentials`), IP-Rate-Limits je Scope (`register`/`login`/`forgot`/`reset`/`verify`), gleiche `open_signup`/Allowlist-Gate wie Google, Registrierungs-Cap nur bei echter Neuanlage. **Auto-Linking (Produktentscheidung)**: loggt sich jemand per Google mit einer Adresse ein, die schon ein E-Mail/Passwort-Konto hat, wird `google_sub` an dieses Konto gehängt statt am Unique-`email` zu kollidieren. **Admin-Benachrichtigung** nur bei E-Mail-Registrierung (`ADMIN_NOTIFY_EMAIL`, Default `martin.pfeffer@celox.io`; datenminimal: E-Mail/Name/Methode/Browsersprache/Zeitpunkt, KEINE IP). **Schemas** `app/schemas/auth.py` (Pydantic + Stärke-Policy). **Frontend**: `features/auth/AuthPanel.tsx` (Login/Registrieren-Tabs + „Passwort vergessen", Stärke-Meter `lib/passwordStrength.ts`, geteilter `StrengthMeter`) auf der Landing neben dem Google-Button; Public-Routes `/bestaetigen` (`VerifyEmailPage`, → CRT-on-Auto-Login) und `/passwort-zuruecksetzen` (`ResetPasswordPage`). **Animationen wiederverwendet**: Login/Verify → CRT-Power-on (`zk-crt-on`-Flag + Full-Nav), Registrierung/Reset-Erfolg → `SparkBurst`-Celebration, Logout → CRT-off. **Datenschutzerklärung** um Abschnitt 6 (E-Mail/Passwort) ergänzt. Tests: `test_passwords.py`, `test_auth_email.py` (Register/Verify/Login/Forgot/Reset, Enumeration, Single-Use-Reset, Auto-Link), Frontend `passwordStrength.test.ts`.
- **Bug-Jagd 2026-07-27 (7 Befunde, alle gefixt + testgesichert)**: **(1) BYOK-Clients leckten** — `get_client(api_key)` baute pro Aufruf einen `AsyncAnthropic` (4 Stellen), geschlossen wurde genau einer (`verify_key`); jeder offene httpx-Pool bleibt liegen → irgendwann Dateideskriptor-Ende. Jetzt geht **jeder** BYOK-Aufruf durch `ai.client_for(api_key)` (async CM, schließt Wegwerf-Clients, gibt den geteilten Projekt-Client unangetastet zurück). Grep-as-a-Test verbietet neue `get_client(api_key)`-Aufrufe außerhalb. **(2) Key-Prüfung war ein Orakel** — `PUT /me/anthropic-key` fragte ungedrosselt bei Anthropic nach; jetzt 5/min/IP (`check_ip_limit`, Scope `byokkey`) + 20/Tag/Konto (`consume_scoped`), und der Format-Check läuft **vor** dem Tagesbudget (ein Tippfehler darf den Besitzer nicht aussperren). **(3) SW-API-Cache überlebte den Kontowechsel** — `zauberkoch-api-v1` hält Rezept-Antworten unter für alle identischen URLs; auf geteilten Geräten sah der Nächste offline die Liste des Vorigen. Jetzt löscht der SW ihn auf `postMessage {type:'zk-clear-api-cache'}`; `lib/apiCache.ts` + Effekt in `App.tsx` feuern **nur bei echtem Identitätswechsel** (`shouldClearApiCache`, gemerkt in `localStorage zk-last-account`) — „bei jedem Login löschen“ würde den Offline-Cache bei jedem Seitenaufruf plätten. **(4) Einheiten spalteten Zutaten** — Merge-Key ist `(name, einheit)`, aber `Stk.` normalisierte anders als `Stk`; jetzt strippt `_unit_key` Punkte und die Map kennt `Esslöffel/Teelöffel/gr/dl/Scheiben/Msp./Pck./Bd.` usw. **(5) `POST /shopping/items` führte gar nicht zusammen** (beim Fund entdeckt) — zweimal „Tomate“ von Hand ergab zwei Zeilen; nutzt jetzt dieselbe Merge-Regel wie der Rezept-Pfad (abgehakte Zeilen bleiben bewusst außen vor). **(6) „Unbegrenzt“-Label war stale** — kommt jetzt aus `me.own_key.active` statt aus dem Generierungs-Store, der es nur beim `saved`-Event lernt. **(7) Profil-Sheet verwarf ungespeicherte Eingaben** — der Seed-Effekt lief bei jeder `me`-Änderung, also auch nach `refreshMe()` beim Key-Speichern; seedet jetzt **einmal pro Öffnen** (`seeded`-Ref). Dazu **(8) Race Konto-Löschung ↔ laufende Generierung**: der Finalizer läuft im Worker-Thread nach dem Modell-Call und schrieb user-gebundene Zeilen → `FOREIGN KEY constraint failed`, wodurch auch der **Cache-Eintrag** verloren ging; jetzt prüft er die Existenz des Kontos und rettet mindestens die bezahlte KI-Ausgabe in den Cache. Der Test reproduziert das Rennen echt (Fake-Stream löscht das Konto zwischen letztem Event und Finalizer) und schlägt ohne den Guard fehl. **postcss** per `overrides` auf ≥8.5.18 gehoben (Dependabot); **react-router 8 bewusst NICHT** mitgezogen — die Lücke betrifft RSC-Mode (hier ungenutzt), ein Major-Sprung fasst den Data-Router an, an dem VT + Mobile-Back hängen.
- **Eigener Anthropic-Key (BYOK, 2026-07-27)**: Nutzer können im Profil einen **eigenen API-Schlüssel** hinterlegen — solange einer aktiv ist, generiert das Konto **ohne unsere Tageslimits** (Nutzer-, Global- und Foto-Scan-Cap), weil die Limits nur unser Budget deckeln. **Speicherung:** AES-256-GCM (`app/services/secretbox.py`, neue Dependency `cryptography`); Wrapping-Key via HKDF aus `BYOK_SECRET`, sonst aus `SESSION_SECRET` mit eigenem Info-Label (dieselben Bytes dürfen nie Session-Signatur **und** Datenschlüssel sein). Zufalls-Nonce je Verschlüsselung; `decrypt()` **wirft nie** — ein unlesbarer Eintrag (Secret rotiert, Zeile manipuliert) zählt als „kein Key“, das Konto fällt auf unseren Schlüssel + Limits zurück statt zu brechen. **Der Klartext verlässt den Server nie:** `/me` und der Export liefern nur `{active, hint}` (letzte 4 Zeichen) — testgesichert. **`byok.key_for(user)` ist DAS Prädikat** für „unbegrenzt“: Limit-Skip und tatsächliche Key-Nutzung lesen dieselbe Funktion, können also nicht auseinanderlaufen. **Validierung** beim Speichern über `ai.verify_key` = `models.list()` (authentifiziert, **kostet keine Tokens**), Format-Check (`sk-ant-…`) davor, damit ein Tippfehler keinen Roundtrip kostet. **Kostenrechnung:** neue Spalte `generations.byok` (Migration `d5e6f7a8b9c0`, zusammen mit `users.anthropic_key_enc/_hint/_set_at`) → `pricing.generation_cost` gibt 0 zurück, damit fremdfinanzierte Tokens nicht als **unsere** Kosten im Dashboard landen. `ai.get_client(api_key)` baut für BYOK einen **Wegwerf-Client** (kein Pool — fremde Credentials sollen nicht prozesslang im Speicher liegen); der `saved`-SSE-Event trägt `unlimited: true` statt eines Countdowns, die UI zeigt „Unbegrenzt — eigener Schlüssel aktiv“. Cache-Verhalten unverändert: BYOK-Ergebnisse landen im geteilten `generation_cache` (Rezepttext, kein Personenbezug) und BYOK-Nutzer bekommen weiter Gratis-Cache-Treffer. Datenschutzerklärung §7 um den Abschnitt „Optionaler eigener API-Schlüssel“ ergänzt (Einwilligung Art. 6 Abs. 1 lit. a, jederzeit entfernbar). Tests: `tests/test_byok.py` (33: Krypto-Roundtrip/Nonce-Frische/Manipulation/rotiertes Secret, Format-Gate, alle Endpunkte, Unlimited-Pfad, Fallback bei unlesbarem Eintrag) + E2E in `e2e/account.spec.ts`. ⚠️ **Alle AI-Einstiegspunkte tragen jetzt `api_key: str | None = None`** (`generate_recipe_events`/`adapt_recipe_events`/`substitute_options`/`fridge_scan`) — Test-Mocks müssen den Parameter akzeptieren.
- **Konto-Selbstbedienung: Export + Löschung (2026-07-27)**: Auskunft/Übertragbarkeit (Art. 15/20) und Löschung (Art. 17) laufen **in der App** statt per E-Mail — beides im Profil-Sheet unter „Deine Daten“. `GET /me/export` liefert **eine JSON-Datei zum Download** (`Content-Disposition: attachment`) mit Konto, Einstellungen, Rezepten (inkl. **soft-gelöschter**, als solche markiert — sie liegen noch da, Verschweigen wäre eine halbe Auskunft), Einkaufsliste, Wochenplan, Nutzungslog; `params_json`/`recipe_json` werden **geparst** ausgegeben, nicht als escapete Strings. `DELETE /me` (CSRF + `{confirm:true}`, bei Passwort-Konten zusätzlich das Passwort → 403) löscht via FK-Kaskade Sessions, Rezepte (damit Favoriten/Plan/Share-Links), Einkaufsliste und Invite-Codes. **Zwei Tabellen hängen an keinem FK und werden von Hand geräumt:** `rate_limits`-Zeilen mit Scope `user:<id>` (SQLite vergibt die höchste id neu → das nächste Konto erbte sonst ein aufgebrauchtes Tageslimit) und der `allowlist`-Eintrag der Adresse. **`generations` werden NICHT gelöscht, sondern entkoppelt** (Migration `c3d4e5f6a7b8`: `user_id` nullable + `ON DELETE SET NULL`) — sonst schrumpften vergangene Monate im Kosten-Dashboard rückwirkend; übrig bleibt „eine Generierung, dieser Tag, diese Kosten“ ohne Personenbezug (Panel/`stats.py` zeigen „gelöschtes Konto“). `/me` liefert dafür neu `has_password` (steuert das Passwortfeld im Dialog). Nach dem Löschen läuft dieselbe **CRT-Abschalt-Animation** wie beim Logout. Datenschutzerklärung Abschnitt 11 entsprechend umgeschrieben (inkl. dem, was bewusst bleibt: entkoppeltes Nutzungslog + parameter-gehashter Rezept-Cache). Tests: `tests/test_account.py` (19, inkl. Export-Robustheit bei kaputtem JSON) — prüft jede Tabelle **einzeln** statt der Kaskade zu vertrauen, weil SQLite FKs nur mit `PRAGMA foreign_keys=ON` erzwingt; E2E `e2e/account.spec.ts`.
- **Drucken / PDF (2026-07-27)**: „Drucken“-Aktion in der Rezept-Detailansicht = `window.print()` + **`@media print` in `base.css`** — kein Dialog, keine PDF-Bibliothek (der Browser kann beides, inkl. „Als PDF sichern“). Der Druck erzwingt **Schwarz auf Weiß** (Dark-Theme-Tokens werden überschrieben, sonst flutet die Seite Farbe), blendet Header/Nav/Footer/Buttons/Stepper/Chips aus, nimmt Karten Rahmen+Schatten, verhindert Seitenumbrüche **in** Schritten (`break-inside: avoid`) und setzt `@page`-Ränder. Im Rezept fliegen die App-Bedienelemente raus (`.zutat__subst`, `.detail__feedback`) und das Notizfeld nur, wenn es **leer** ist (`:has(textarea:placeholder-shown)`) — eine geschriebene Notiz gehört aufs Küchenblatt. Neue Icons `print`/`download`.
- **E2E-Gotcha (2026-07-27)**: `playwright.config.ts` setzt **`serviceWorkers: 'block'`** — der Preview-Build registriert den PWA-SW, und **von ihm ausgelöste Requests umgehen `page.route()`**; Mocks fielen dann auf ein nicht laufendes Backend durch (502). Kostete beim Druck-Test eine Weile Suche.
- **Keine KI-Bilder** — kuratierte SVG-Motive pro Länderküche.
- **Brand-Assets (2026-07-12)**: Alle statischen Icons/Social-Images liegen in `frontend/public/` und werden aus EINEM Master-Chef-Hut-Logo (Mint-Welt, identische Geometrie zum in-App `logo`-Glyph) generiert: `npm run gen:assets` (`scripts/generate-assets.mjs`, Playwright-Chromium-Raster, kein `sharp`; ICO handgeschrieben). Output: `icon.svg`, `favicon.ico` (16/32/48), `favicon-16/32.png`, `apple-touch-icon.png` (180, **opak** — iOS rendert Transparenz schwarz), `icon-192/512.png` (purpose `any`), `icon-maskable-512.png` (**opak, Full-Bleed**, Motiv ≤80 % Safe-Zone), `og-v2.png` (1200×630). **Farb-Single-Source-of-Truth** = `COLORS`-Objekt im Script (spiegelt `tokens.css`-Mint: bg `#14531f`, Hut `#b1f0b2`, Band `#a0cfd3`, Funke `#f0b429`). Theme-Wechsel künftig = `COLORS` ändern + `npm run gen:assets`. **OG/Twitter versioniert** (`og-v2.png`, nicht `og.png` — FB/LinkedIn cachen aggressiv). Preview: `scripts/asset-preview.html` (hell+dunkel). `manifest.webmanifest`: `any`+`maskable` getrennt, `theme_color`/`background_color` = `#f7fbf1`. Die **dynamischen** per-Rezept-OG-Bilder (`/api/v1/share/…/og.png`, Backend `og_image.py`) sind davon unberührt. iOS-Splash-Screens bewusst nicht generiert.
- **PWA**: Service Worker mit versioniertem Cache `zauberkoch-vN` (aktuell **v112**) — bei jedem App-Shell-Change bumpen und Version hier nachführen. Favoriten offline lesbar. **Offline-Indikator (2026-07-15):** `useOnline()` (`state/useOnline.ts`, `useSyncExternalStore` auf `online`/`offline`-Events) blendet im Shell eine unaufdringliche `.offline-bar` (aria-live, `wifiOff`-Glyph, i18n `common.offline`) unter dem Header ein — kein blockierender Fehler, der SW liefert Shell + Favoriten/Rezepte weiter aus.
- **Footer-Kolophon + „Neues Rezept“ per Logo (2026-07-26)**: Über den Rechtslinks steht ein **Kolophon** (`.colophon` in `App.css`, Texte unter `colophon` in `de.ts`): zwei umrandete Karten (GitHub-Repo mit Octocat, PayPal-Spende `martin.pfeffer@celox.io`) im 1-/2-Spalten-Grid (ab 520 px zweispaltig), darunter „Mit ♥ in Berlin gemacht“ mit sanft schlagendem Herz (`@keyframes colophon-beat`, 2,4 s). Hover-Lift nur unter `(hover:hover) and (pointer:fine)`, `prefers-reduced-motion` schaltet Herzschlag **und** Karten-Transition ab. Der Octocat lebt jetzt in **`components/icons/GithubMark.tsx`** (bewusst außerhalb der monochromen 24×24-Registry — Marken-Logo wird verbatim gezeichnet) und wird von Kolophon **und** `AiBudgetNotice` genutzt. **Header-Logo = „Neues Rezept“:** ein Klick auf „Zauberkoch“ navigiert nicht nur nach `/`, sondern verwirft eine **fertige** Generierung (`getGeneration().phase === 'done'` → `cancelGeneration()` in `App.tsx`) — sonst zeigte der Wizard nur wieder das alte Ergebnis. Bewusst **nur** bei `done`: eine laufende (bezahlte) Generierung darf das Logo nie abbrechen (dafür gibt es „Abbrechen“). Gelesen wird über den nicht-reaktiven `getGeneration()`-Getter, damit die Shell nicht pro gestreamtem Token neu rendert.
- **Conjure-Stage bleibt groß (2026-07-26)**: Die Kessel-/Shaker-Szene schrumpfte früher in dem Moment, in dem der Titel eintraf, auf einen 72-px-Banner — die Signatur-Animation war vorbei, bevor sie wirkte. Der Kompakt-Morph ist **entfernt** (`SCENE_SIZE`/`COMPACT_SIZE`, `compact`-Flag und alle `.conjure--compact`-Regeln sind weg): 320 px stehen die ganze Generierung über dem aufbauenden Rezept. Zweite Zeile ist der **Abbrechen-Hinweis**, bis Schritte streamen — erst dann der Zähler (`pillProgress`), weil die Phasenzeile die Zutatenzahl bereits nennt (sonst stand dort „2 Zutaten sind schon drin …“ über „2 Zutaten“).
- **Kolophon-Genie (2026-07-28)**: Hover auf eine der drei Kolophon-Karten lässt einen **Punkte-Schwarm aus dem Icon der Karte aufsteigen**, der sich hinter dem Footer zur passenden Figur fügt — Octocat (GitHub), Espressotasse mit Dampf (Spende), fünf goldene Sterne (Bewertung). Verlassen spielt dieselbe Bewegung rückwärts, die Figur wird ins Icon zurückgezogen. **Technik** wie das Octocat-Feld auf celox.io: Silhouette in ein Offscreen-Canvas rastern (`RES`=520), Alpha-Kanal auf einem gejitterten Raster abtasten (`genieShapes.ts`, Ergebnis gecacht) — **aber ohne Three.js**: ein Footer-Schmuck darf keine 150 kB + WebGL-Kontext kosten, 2D-Canvas malt ein paar tausend `fillRect` mühelos. **Genie-Auftritt statt Wolke (überarbeitet 2026-07-28):** Jedes Partikel fliegt eine **quadratische Bézier-Kurve, deren Kontrollpunkt weit ÜBER dem Icon liegt und für alle fast derselbe ist** — sie verlassen die Flasche als eine schmale Säule und fächern erst kurz vor dem Ziel zur Figur auf. Der geteilte Kontrollpunkt ist der ganze Trick; die erste Fassung (gerade Bahn Icon→Ziel plus seitlicher Swirl) sah aus wie „Punkte erscheinen“. Dazu: Dauer **1150 ms statt ~600** (der Aufstieg soll eine Bewegung sein, die man ansieht, kein Zustand, den man fertig vorfindet), Fortschritt **linear in der Zeit** (die Kurve steckt pro Partikel im Smoothstep — weicher Start UND weiche Ankunft), Swirl senkrecht zur **Tangente** der Bahn, Stagger `MAX_DELAY`. **Höher gehängt + Funkeln (2026-07-28):** Das Feld reicht jetzt **230 px über das Kolophon** (vorher 90) und die Figuren sitzen bei `cy` **0,4**, die Sternreihe bei **0,262** (2026-07-29: gemessen liegt die Mitte des „Überrasch mich“-Buttons bei 0,307 — die Reihe genau dorthin zu setzen versteckte den mittleren Stern hinter dem Button, knapp darüber stehen alle fünf frei und krönen ihn) statt 0,5/0,575 — sie stehen damit im freien Raum über den Karten, auf der Wizard-Seite hinter dem „Überrasch mich“-Button, wo sie Platz haben und sich nicht hinter Panels verstecken. Maske entsprechend nachgezogen (`#000` 9 %–76 %). **Funkeln:** zusätzlich zur wandernden Helligkeitswelle (Tiefe auf Figuren 0,28→**0,42**) glitzern einzelne Punkte per `sin^8` — meist dunkel, mit kurzem scharfem Peak. Zwei Lehren aus dem Bild: (1) Ein zum Peak **vergrößertes Quadrat liest sich als Klotz**, nicht als Funke — deshalb wird ab `SPARK_CROSS_MIN` ein **vierarmiger Blitz** gezeichnet (zwei dünne Rechtecke), und die Quadrat-Vergrößerung bleibt klein. (2) Selbst `sin^8` liegt ~17 % der Zeit nahe am Peak — mit *allen* Punkten als Kandidaten wurde die Katze zu einem Feld aus Plus-Zeichen. Funkeln ist deshalb eine Eigenschaft **weniger** Punkte (`SPARKLER_SHARE_FIGURE`=0,12, Staub 0,06). `globalAlpha` wird geklemmt, weil Glints über 1 hinausschießen würden. Kosten unverändert: 120 fps im Staub wie mit jeder Figur, bei 4× gedrosselter CPU. ⚠️ Der E2E-Morph-Test tastet ein **Band an der Sternposition** ab — bei einer Verschiebung von `cy` mitziehen.
**Ruhender Staub statt leerem Feld (2026-07-28):** Ohne Hover liegt jetzt ein **feiner, driftender Punktestaub** hinter dem Footer (`DUST_COUNT`=380, halb so hell wie eine Figur, weite langsame Wanderung statt bloßem Atmen). Hover **verdichtet** ihn zur Figur — die Zusatzpunkte steigen wie gehabt aus dem Icon auf; Verlassen **zerstreut** sie wieder zu Staub, der Überschuss fließt in das zuletzt gehoverte Icon zurück (deshalb überlebt `origin` das Verlassen: eigener State im `Colophon`, nur `shape` wird null). Das ist derselbe Morph-Mechanismus, „Staub“ ist nur eine weitere Anordnung. **Zwei Repel-Stärken:** über dem Staub darf der Zeiger kräftig rühren (R 105/34), an einer Figur nur sanft teilen (R 62/9) — sonst stanzt der auf der Karte *ruhende* Zeiger ein dauerhaftes Loch mitten in die Figur (passierte mit einem Wert für beides). **Dauerlauf ist gegated:** die rAF-Schleife läuft nur, solange das Feld im Viewport ist (IntersectionObserver) **und** der Tab sichtbar ist (`visibilitychange`) — beides verifiziert (Canvas unterhalb des Folds → kein Malen; `document.hidden` → kein Malen). Schleifensteuerung und Umzielen sind zwei getrennte Effekte, sonst würde jeder Hover Observer neu aufsetzen und die laufende Transition abschneiden. Kosten: 120 fps im Staub **und** mit gesetzter Figur, auch bei 4× gedrosselter CPU. **Sternreihe** von `cy` 0,64 auf **0,575** — unter dem Kolophon hängend wirkte sie abgehängt, jetzt liegt sie über der Bewertungskarte. **Kartenwechsel = Morph (2026-07-28):** Von einer Karte direkt auf die nächste zu fahren tauschte die Figur vorher **schlagartig** (Partikel wurden neu gebaut, während der Fortschritt schon auf 1 stand). Jetzt trägt **ein einziger Mechanismus** alle drei Übergänge: jedes Partikel hat Start-, Kontroll- und Zielpunkt, ein Fortschritt schiebt es über die Kurve. Erscheinen = „alle kommen aus der Flasche“, Verschwinden = „alle gehen zurück hinein“, Figurwechsel = „jedes fliegt weiter, nur zu einer neuen Adresse“. Ein **mitten im Flug umgelenktes** Partikel startet seine nächste Kurve schlicht dort, wo es gerade ist (`px/py` wird jeden Frame mitgeschrieben) — deshalb überlebt schnelles Hin- und Herfahren. Zählt die neue Figur **mehr** Punkte, steigen die zusätzlichen aus dem *neu* gehoverten Icon auf; zählt sie weniger, fließen die überzähligen dorthin zurück und verlöschen. Die Bahn beim Morph ist seitlich ausgestellt (`MORPH_ARC`, Vorzeichen pro Partikel) plus leichter Lift, damit der Schwarm sich kreuzt statt als Block zu rutschen. **Farbwechsel** (Tinte↔Gold) überblendet mit: bis zu drei Farben sind im Spiel, gruppiert gezeichnet — drei `fillStyle`-Wechsel pro Frame statt einer pro Partikel. **Wichtig fürs Verhalten:** `pointerleave` hängt am **Container**, nicht an den Karten — sonst startete der Weg zur Nachbarkarte erst ein Einziehen, das der folgende `pointerenter` wieder aufheben muss (die Figur zuckte). Gemessen: 150 ms nach dem Wechsel liegt die Alpha-Masse im Zielband bei 57, im Endzustand bei 1083 — bei einem harten Umschalten wäre sie sofort dort (E2E-Test prüft genau das). **Ruhezustand animiert wie bei celox:** Atem-Drift (`sin/cos` mit Phase, 1–3,4 px), eine **Helligkeitswelle, die über die Figur wandert** (liest die eigene Ziel-x, deshalb wandert sie, statt zufällig zu funkeln), langsames Schwanken der ganzen Figur, und **Cursor-Abstoßung**: Punkte im Radius werden weggeschoben (`f²`), die Verschiebung heilt geglättet zurück und die verdrängten Punkte **leuchten dabei auf** — ein Glühen folgt dem Zeiger. Alpha wird pro Partikel gesetzt (billig), Farbe nur zweimal pro Frame (zwei Pässe ink/accent). **Gemessen**: 120 fps, p95-Frame 9,2 ms — auch mit 4× gedrosselter CPU unverändert; die rAF-Schleife läuft nur, solange eine Figur steht. **Fallen, die Zeit gekostet haben:** (1) `<canvas>` ist ein **replaced element** — `left/right` + `width:auto` streckt es NICHT, es behält 300×150; deshalb steckt es in einem gestreckten Wrapper-Div (genau wie bei celox). (2) Der **16×16-GitHub-Mark füllt die Scheibe und stanzt die Katze aus** — als Punktfeld ergibt das einen Ring; verwendet wird der positive Octocat-Umriss (256er-viewBox, dieselbe Silhouette wie celox). (3) Die Sternreihe muss **in die Sample-Box passen**, sonst schneidet das Rastern die äußeren Sterne ab — Dichte kommt über `RES`, nicht über größeres Zeichnen. **Gated:** nur `(hover:hover) and (pointer:fine)` **und** ohne `prefers-reduced-motion` — sonst wird weder Canvas noch Chunk montiert (E2E prüft das). Das Feld wird beim **Sichtbarwerden** scharfgemacht (IntersectionObserver + `requestIdleCallback`-Prewarm), sonst startete die erste Animation erst nach dem Lazy-Chunk-Load sichtbar verspätet. Die rAF-Schleife **stoppt vollständig**, sobald das Feld leer ist. Karten werden während der Animation leicht transluzent (`color-mix`), sonst läge die Figur hinter drei deckenden Panels. `Colophon.tsx` ist dafür aus `App.tsx` herausgelöst (braucht Hover-State + Emitter-Messung an einem Ort).
- **Google-Bewertung im Kolophon (2026-07-28)**: dritte Karte im Footer-Kolophon (`.colophon__card--review`, Ziel `https://g.page/r/CXgdRV3QysvxEBM/review`, goldener `star` auf neutralem Container — so bleiben alle drei Marken unterscheidbar: dunkler Octocat, grünes Herz, goldener Stern). Im 2-Spalten-Grid **spannt** sie beide Spalten (`grid-column: 1/-1`), sonst stünde die dritte Karte halbbreit verwaist. Der Link existiert **zusätzlich** im `SupportPrompt` — der erscheint aber einmalig nach 5 Rezepten und lässt sich dauerhaft abschalten; das Kolophon ist die bleibende, jederzeit erreichbare Stelle. E2E in `smoke.spec.ts` prüft alle drei Footer-URLs plus `target=_blank`/`rel=noopener` (ein Tippfehler in der URL wäre sonst unbemerkt).
- **Icon-System (2026-07-12)**: KEINE Emojis in der UI — alle Symbole über `<Icon name="…" size={…} />` aus `src/components/icons/` (52 Custom-SVG-Glyphen, 24×24-Grid, Registry typisiert via `IconName`; Stil-Spez `ILLUSTRATION_STYLE.md` im Repo-Root). Funktionale Icons monochrom `currentColor` (Sekundärformen per opacity), Marken-Icons `logo`/`wand` mit Tokens (`--c-primary`/`--c-tertiary`/`--icon-gold`). Größen: Nav 24 · Header-Logo 28 · Text-Buttons 18 · Inline-Stats 15–16 · Chip-Affixe 13–14 · Empty-States 48+. Region-Header-Icons stehen als `icon`-Key in `strings.cuisineRegions`. **Bewusste Ausnahme**: die ~110 Zutaten-Emojis der Kessel-Animation (`lib/zutatEmoji.ts`, `ConjureStage.tsx`) bleiben Emojis. Neue Icons: Glyph in `glyphs.tsx` + Testliste in `icons.test.ts`; QA-Galerie: `scripts/icon-gallery.tsx`.
- **Karten-Motive**: Flache Vektor-Illustrationen pro Gericht-/Glas-Typ in `RecipeMotif.tsx` (35 Kategorien × Varianten = **58 Grafiken**, deterministisch per Titel-Hash `variantFor`: 13 Glas-Typen highball/tumbler/coupe/tiki/martini/wine/flute/mule/shot/mug/beer/margarita/punch + 22 Gericht-Typen pasta/bowl/suppe/pfanne/pizza/salat/burger/fisch/steak/dessert/taco/auflauf/pancakes/sandwich/sushi/kuchen/eis/spiess/dumpling/wrap/brot; Matcher `motifForRecipe`, Cocktails matchen über das `glas`-Feld der Listen-API). Neue Motive IMMER über den Skill `.claude/skills/recipe-motifs/` erzeugen (Stil-Spezifikation + Generator-Prompt) — keine KI-Rasterbilder. **Alle Motive sollen die 120×120-viewBox ähnlich füllen** (ein zu kleines/klein-wirkendes Motiv fällt neben den anderen auf — z. B. wurde `shot` nachträglich hochskaliert); zum Prüfen `npm run measure:motifs` (`scripts/measure-motifs.tsx`, Headless-Chromium-`getBBox` je Motiv+Variante, sortiert nach Höhe — Ausreißer nach unten = zu klein). Nach Motiv-Änderungen die OG-PNGs neu erzeugen: `npm run export:motifs` (rendert nach `backend/app/assets/motifs/` für den dynamischen Rezept-OG-Renderer). **Kompakte Listen-Icons (2026-07-15):** In kleinen Leading-Slots (Wochenplaner-Rezeptpicker + Plan-Tag-Zeilen) fielen die unterschiedlichen viewBox-Füllungen als „unterschiedliche Icon-Größen" auf. Fix ohne Motiv-Chirurgie: (a) `RecipeMotif`-Prop **`fit`** skaliert per `MOTIF_FIT`-Map (aus `measure:motifs` abgeleitet, `clamp(96/max(w,h),0.8,1.38)`) die größte Motiv-Dimension einheitlich auf ~96/120 → Rohspanne 56–115 kollabiert auf 77–96; (b) einheitliche gerahmte **`.motif-tile`** (48px, `--c-surface-container-high`, `overflow:hidden`) als Leading-Media-Slot (`.picker-row` in `PlanPage.tsx`, CSS in `recipe.css`). Die großen Karten-/Hero-Motive bleiben unskaliert (`fit` nur für Tiles). Bei `MOTIF_FIT`-Pflege: neue Motive dort ergänzen (Default 1).
- **Dauer-Formatierung** immer über `strings.units.duration(min)` (de.ts): Min. → h → Tage („3 Tage" statt „4320 Min."). **Küchen-Chips personalisierbar**: `preferences.kuechen` (Backend-Preferences, max 40, leer = App-Defaults), Editor = `CuisineSheet` (✏️-Chip im Wizard-Schritt 1, ~110er-Katalog in `strings.cuisineRegions`, Suche + Custom-Einträge).
- **Gericht-Art (Mahlzeit-Typ, 2026-07-12)**: Kochen-Modus hat eine optionale **Gericht-Art**-Dimension (`gericht_typ`-Param, `strings.gerichtTypen` = Frühstück/Hauptgericht/Dessert/Snack/Vorspeise/Beilage/Meal-Prep) für größeres Spektrum. Analog zu `drink_typ`: optionale Chip-Reihe **über** der Küche in Wizard-Schritt 1 (Default „egal", kein neuer Schritt), fließt lowercased in **Prompt** (`recipe_v1` Kochen-Zweig, kein Versions-Bump) + **Cache-Key** (`cache_relevant`). **Persistiert**: indizierte Spalte `recipes.gericht_typ` (Alembic `c8a3d9e21f04`, aus Params gesetzt — nicht aus AI-Output; Adapt/Legacy/Überrasch = leer/unklassifiziert), in der List-API exponiert + server-seitig filterbar (`?gericht_typ=`). **Filter-Chips** (client-seitig, nur real vorhandene Typen in kanonischer Reihenfolge) in **Favoriten + Verlauf**; Galerie-Filter für v1 zurückgestellt (Feld ist ausgeliefert). **Stimmigkeit Gericht-Art × Küche (`lib/mealCompat.ts`, 2026-07-12):** zweiseitige Chip-Sperre — unstimmige Kombis (z. B. Frühstück + Pizza) sind inaktiv/ausgegraut. **Konservativ & keyword-basiert:** Länderküchen sind IMMER kompatibel (jede Kultur hat alle Gänge); nur Gericht-Form-Stile werden für eindeutig absurde Gänge gesperrt (`pizza/pasta/bbq|smoker/sushi|sashimi/tapas|pintxo/mezze` → kein Frühstück/Dessert; `ramen|nudelsupp` → kein Dessert). Keyword-Match deckt Katalog + Freitext ab; der einzige nicht-sperrbare Pfad (Freitext nach Gericht-Art-Wahl) wird per Effekt abgeräumt. `Chip` hat dafür ein `disabled`-Prop.
- **Drinks-Modus (2026-07-12)**: UI-Label heißt überall „Drinks" (Wizard, Filter, 18+-Dialog); der interne Modus-Wert bleibt `cocktail` (DB/API/Cache/Motive — NICHT migrieren). Wizard ist modusabhängig: Schritt 1 = **Drink-Typ** (`drink_typ`-Param, `strings.drinkTypes`, Freitext) statt Küche, Schritt 2 = `strings.tastesCocktail`; Küchen-Personalisierung nur im Kochen-Modus. Marketing-Wording („Rezepte & Cocktails" in Tagline/SEO) bewusst unverändert.
- **Rahmenbedingungen-Persistenz (2026-07-12)**: Die Wizard-„Feinschliff"-Diät-Flags (vegetarisch/vegan/glutenfrei/laktosefrei/proteinreich/ketogen) haben **eine Quelle = das Profil** (`me.preferences`, server-persistiert, cross-device). Die Feinschliff-Schalter **lesen aus und schreiben in** die Preferences (optimistisches `queryClient.setQueryData(['me'])` + `putPreferences`, `savePrefs()` in `GeneratePage`) — sie spiegeln also das Profil und persistieren überall; das Backend OR-merged sie ohnehin in jede Generierung (Cache-Key nutzt die gemergten Flags, Merge VOR `params_hash`). **Max-Zeit + Schwierigkeit** haben kein Profil-Äquivalent → per `localStorage` gemerkt (`zk-wiz-maxzeit`/`zk-wiz-schwierigkeit`, überleben Reload/Navigation). **Portionen** aus `standard_personen` geseedet (GeneratePage mountet erst nach `meLoading`, daher zuverlässig). KEIN lokaler Diät-`useState` mehr im Wizard (war der Bug: startete false, ignorierte das Profil, resettete bei Navigation).
- **Zutaten-Autocomplete (2026-07-26)**: Kühlschrank-Feld (Wizard) und Vorräte (Profil-Sheet) teilen sich **`components/ui/ZutatInput.tsx`** — Eingabefeld + Vorschlagsliste + entfernbare Badges. Katalog: **`lib/zutatKatalog.ts`** (~250 Zutaten in 9 Kategorien, `TOP_ZUTATEN` als Fokus-Vorschläge, reine `matchZutaten()`-Suche: Präfix-Treffer vor Teilstring, `foldZutat()` ist **umlaut-/diakritika-tolerant** → „moehre“ findet „Möhre“, „creme fra“ findet „Crème fraîche“). **Freitext bleibt immer erlaubt**; Dedupe fold-insensitiv (kein „tomate“ neben „Tomate“). **Bewusst KEIN `<datalist>`** (nicht token-stylebar, browser-abhängig, auf iOS unzuverlässig) — stattdessen ARIA-Combobox-Pattern (↑/↓/Enter/Escape, `aria-activedescendant`), Auswahl per `onPointerDown` **vor** dem Blur, Backspace bei leerem Feld entfernt das letzte Badge. **Badges stehen ÜBER dem Feld** (wie im `CuisineSheet`): die Liste klappt nach unten auf und würde darunterliegende Badges verdecken — ihr X wäre beim Tippen unerreichbar. Das X ist ein **echter Button** mit `aria-label="<Zutat> entfernen"` (32 px sichtbar, 44 px Trefferfläche via `::before`-Overlay). ⚠️ **Test-Gotcha:** nach dem Entfernen bleibt das Badge während der AnimatePresence-Exit-Animation **noch im DOM** — E2E-Assertions brauchen ~600 ms Wartezeit, sonst sieht man fälschlich „nicht entfernt“. Tests: `lib/zutatKatalog.test.ts` (Katalog-Integrität, Duplikate, Ranking, Umlaut-Suche).
- **Vorräte**: `preferences.vorraete` (Pflege im Profil-Sheet) erscheinen im Wizard-Kühlschrank-Schritt als **vorausgewählte Chips** (Tap = für diesen Lauf abwählen); keine eigene Nav-Rubrik. **Einkaufsliste hat zwei Ansichten** (Segmented, `zk-shopping-view` in localStorage): „Liste" = aggregierte Abhak-Liste, „Nach Gericht" = filterbare Rezeptliste mit aufklappbaren Zutaten + „Zutaten zur Liste" — bewusst OHNE Häkchen-Sync zwischen den Ansichten (Aggregation verschmilzt Mengen).
- **Growth-Features (2026-07-12)**: (a) **Probier-Zauber** `POST /recipes/try` (Landing, ohne Login) — Kostendeckel strikt: Cache-Hits gratis/unbegrenzt, Live nur mit 2/Tag pro IP UND globalem `DAILY_LIMIT_ANON` (env, Default 15, zählt in den Global-Deckel); nur Kochen, nichts persistiert. (b) **Invite-Codes**: 5 pro User (`GET /me/invites`, Profil-Sheet), Signup = Allowlist ODER gültiger Code (via signiertem OAuth-State-Cookie, Einmal-Nutzung). **Admin** kann zusätzlich beliebig viele Codes anlegen (`admin.py`: `GET/POST /admin/invites` Batch 1–50, `DELETE /admin/invites/{code}` nur für ungenutzte → sonst 409; `created_by`=Admin, gleiche `invites`-Tabelle). Admin-Panel-Sektion „Invite-Codes": Stepper + Erzeugen, Frisch-Banner mit Copy/„Alle kopieren", Liste mit Status (offen / eingelöst von `<email>`) + Copy/Revoke. (c) **Galerie/SEO**: `public_listed`-Opt-in im ShareDialog → `GET /share/discover` + `/share/daily` (Datums-Rotation, 0 Kosten) + dynamische `/sitemap.xml` (eigene nginx-Location). (d) **Story-Bild** `GET /share/{token}/story.png` (1080×1920). (e) **Wochenplaner** `/plan`-API + 5. Nav-Rubrik, „Woche → Einkaufsliste" über `merge_recipe_into_list`. (f) **Substitution** `POST /recipes/{id}/substitute` (Mini-Call). (g) **Sprachsteuerung** im Koch-Modus (Web Speech, „weiter/zurück/beenden"). (h) **Kühlschrank-Scan** `POST /recipes/fridge-scan` (Vision, 5/Tag/User persistent via `consume_scoped`, Client-Downscale auf 1024px). (i) **Support-Nudge (2026-07-26)**: dezenter Dialog nach `FIRST_THRESHOLD`=5 generierten Rezepten (danach still, erst nach `REPEAT_EVERY`=25 weiteren nochmal) — Spenden (PayPal `martin.pfeffer@celox.io`), Feedback (`mailto:martin.pfeffer@celox.io`), Google-Bewertung (`g.page/r/CXgdRV3QysvxEBM/review`). Rein client-seitig, **kein Tracking, keine neue Datenverarbeitung** (Zähler nur in `localStorage`): `state/supportPrompt.ts` (reine `shouldPrompt`-Logik + externer Store via `useSyncExternalStore`; `recordGeneration()` im Generation-Store-`onSaved`, also nur eingeloggte Generierungen — der Landing-Taster läuft über `tryRecipe` am Store vorbei und zählt nicht), Komponente `SupportPrompt.tsx` (nur bei `me` in der Shell). Aktion ODER „Nicht mehr fragen“ = dauerhaft still (`zk-support-off`); „Vielleicht später“ = Wiedervorlage. Neue Icons `heart`/`mail`. Bewusst nicht-manipulativ (Projekt-Regel „Sog-Ebene dezent“). Tests: `state/supportPrompt.test.ts` (happy-dom, Gating + Persistenz). (j) **KI-Budget-Hinweis + Share-Intro (2026-07-26)**: **(1)** Ist das *Projekt*-Budget erschöpft (`daily_limit_global`/`daily_limit_anon`), zeigt `features/support/AiBudgetNotice.tsx` einen hochwertigen Hinweis (Icon, Titel, Erklärung Open-Source + Spendenfinanzierung, **konkrete Rückkehr-Uhrzeit** aus `retry_after`, PayPal-Button, GitHub-Card mit Octocat, „Später erneut versuchen“, „Mehr erfahren“). **Vier Ton-Varianten** in `de.ts` unter `budget` (`short`/`standard`/`warm`/`fun`), umschaltbar über die Konstante `VARIANT` — aktiv ist `standard`. **Bewusste Trennung:** ein *persönliches* Tageslimit (`daily_limit_user`) zeigt weiterhin den neutralen Snooze-Hinweis **ohne** Spendenbitte (nur um Geld bitten, wenn wirklich das Projektbudget die Ursache ist) — bekommt aber ebenfalls die echte Uhrzeit. Doppelte Bitten vermeidet `noteSupportAskedElsewhere()`: der generische `SupportPrompt` wird zurückgestellt, wenn der Budget-Hinweis schon gefragt hat. **(2)** **Share-Intro**: geteilte Links (`/r/:token`) spielen eine Intro-Animation, **global im Admin-Panel umschaltbar** — `app_settings.share_intro` (`motif` | `crt` | `off`, Migration `b7c8d9e0f1a2`, Config-Default `motif`), im `_FIELDS`-Mapping von `limits.py` und in `GET/PATCH /admin/limits` (Literal-validiert). Der Wert reist **öffentlich** in der `GET /share/{token}`-Antwort mit (die Seite kennt ihn sonst ohne Admin-Login nicht). `features/share/ShareIntro.tsx`: `motif` = große Kessel/Shaker-Bühne (je nach `mode`, SVGs aus `ConjureStage` wiederverwendet) mit Orbit-Emojis + Titel-Reveal, **3 s**; `crt` = die Login-Power-on-Animation; `off` = sofort Inhalt. **Robustheit (Mobile zuerst):** ein einzelner Timer entscheidet über „fertig“ (nie ein Animations-Callback — ein gedrosselter Tab könnte den Besucher sonst aussperren), Overlay `position:fixed` + dvh-Maße (URL-Bar-Toggle kann nichts verschieben), Tap/Escape überspringt, `prefers-reduced-motion` → kurzer statischer Frame. Das Rezept rendert **hinter** dem Intro, ist also fertig gemalt, wenn es abhebt. Gemessen: motif ≈3,5 s, reduced ≈1 s, crt ≈0,5 s, off ≈0,15 s; kein horizontaler Overflow auf 430/844/1280 px.
- **Shell-Caching (2026-07-12)**: `index.html` MUSS mit `Cache-Control: no-cache` ausgeliefert werden (nginx `location = /index.html` — Header muss wegen try_files-Internal-Redirect in GENAU dieser Location stehen) und der SW holt Navigationen mit `fetch(request, {cache:'reload'})`. Ohne beides pinnt der heuristische Browser-HTTP-Cache Nutzer nach Deploys auf die alte Shell („ich sehe keinen Unterschied").
- **Interne Navigation**: react-routers **Data-Router** mit eingebauten View Transitions — JEDE Navigation trägt `viewTransition`: `<Link/NavLink viewTransition>` (Tabs/Header/Footer), `navigate(to, {viewTransition:true})` (Karte→Detail), Zurück (Browser + In-App) = `navigate(-1)`. Shared-Naming via `useViewTransitionState('/rezept/<id>')`. **Nie ein echtes `<a href>`** (Full-Reload → killt die laufende Generierung). Der frühere `useViewTx().go()`/`ViewTransitionProvider`/Navigation-API-Listener ist entfernt (s. Motion-Konvention oben). Hintergrund-Indikatoren: `GenerationBar` (Balken unter dem Header) + `GenerationPill` (beide in `GenerationPill.tsx`, lazy).
- **Tooltips (2026-07-13)**: ergänzende Button-Erklärungen via `<Tooltip text={t('tips.…')}>` (`components/ui/Tooltip.tsx`) — reines CSS, nur Desktop (`hover:hover and pointer:fine`), `role="tooltip"` für Screenreader, `prefers-reduced-motion`-Guard; alle Texte in `de.ts` unter `tips`. Verwendet an Wizard- (Überrasch/Rahmenbedingungen/Zaubern/Küche-Anpassen/Kühlschrank-Scan) und Detail-Aktionen (Favorit/Einkauf/Teilen/Kopieren/Koch-Modus/Anpassen/Gekocht).
- **Legal (2026-07-12)**: `/impressum`, `/datenschutz`, `/nutzungsbedingungen` (Seiten in `src/pages/legal/`, bewusst außerhalb von de.ts — einsprachige Rechtstexte), Footer-Links auf jeder Seite + Consent-Hinweis am Login. Datenschutz-Kernaussagen: nur technisch notwendige Cookies (kein Banner nötig, § 25 Abs. 2 TDDDG), Anthropic = AV mit **SCCs** (kein DPF!), Fotos werden nicht gespeichert, Konto-Löschung per E-Mail. Bei neuen Datenverarbeitungen (Tracker, neue Dritt-Dienste, neue Datenarten) MUSS die Datenschutzerklärung aktualisiert werden.
- **SEO/OG**: `index.html` trägt die Root-Meta (Description, Canonical, OG/Twitter mit statischem `/og.png`, JSON-LD WebApplication) in einem Sentinel-Block `<!-- zk:root-meta:start/end -->` — `share.py` strippt ihn auf `/r/{token}`-Seiten und ersetzt auch den `<title>` (sonst lesen Crawler die Root-Tags statt der Rezept-Tags). Das Thumbnail `frontend/public/og.png` (1200×630) wird aus der Design-Quelle `docs/og-root.html` via Headless-Chromium-Screenshot (1200×630) gerendert — bei Änderungen dort neu rendern.
- **Kein horizontaler Scroll**: `html, body { overflow-x: clip }` (base.css) + Header ist schrumpffähig (Logo-Ellipsis, enge `.shell__actions`-Gaps < 430 px). Theme-Wechsel = **Circular Reveal**, echte View Transition auf allen Geräten, **600 ms mit LINEAREM Radius** (Radius 0 → `hypot` zur entferntesten Ecke, auf Touch + 140 px für den Snapshot-Viewport, der mobil die Browser-UI mit überdeckt). **Warum linear:** Die Animation läuft **auf dem Compositor** — gemessen 2026-07-21, sie malte 30 Frames weiter, während der Haupt-Thread 300 ms hart blockiert war. Haupt-Thread-Last und Raster-Stalls scheiden als Ursache für „unregelmäßigen Ablauf“ also aus; die Ungleichmäßigkeit steckt allein in der Kurve. celox' `cubic-bezier(.22,.08,0,1)` schwankt in der Radiusgeschwindigkeit rund zehnfach (Steigung 0,36 → 3,6: 15 % Radius in den ersten 10 % der Zeit, 53 % bei 20 %, fertig bei 80 %) — **auf echter Hardware als unregelmäßig bewertet und deshalb hier NICHT übernommen**, obwohl celox sonst die Vorlage ist. Ein konstanter Radius wirkt trotzdem weich beschleunigend, weil die abgedeckte **Fläche** mit r² wächst; gemessene Abdeckung je 10 % der Zeit: 8/19/30/41/54/65/77/88/99/100 %. CSS-Seite wie celox (Default-Crossfade aus, new oben, `backdrop-filter` während der Transition aus). **Gegen das Stottern wirkt zusätzlich:** (a) **keine React-Arbeit im Animationsfenster** — im VT-Callback wird NUR `data-theme` gesetzt (alle Farben sind CSS-Custom-Properties), `setTheme()` läuft erst in `vt.finished`; das Sonne/Mond-Icon ist CSS-getrieben (beide Glyphen gerendert, `:root[data-theme]` blendet um), damit kein Re-Render nötig ist — verifiziert: **0 Long Tasks zwischen 40–950 ms**, vorher lagen dort Passive Effects + framer-Layout-Messung; (b) `delay: 32ms` + `fill:'backwards'` schiebt URL-Bar-Toggle und React-Commit VOR die Animation; (c) alle `100dvh`-Höhen werden für die Dauer auf px eingefroren (`--zk-vt-vh`, `html.zk-theme-vt` in base.css); (d) Endradius +8 % Reserve, falls die URL-Leiste einfährt und der Viewport wächst. `zk-theme-vt` scoped die Reveal-Regeln, schaltet den Header-`backdrop-filter` ab und nimmt Header/Nav aus dem VT-Hold. **Der frühere mobile-only `themeRipple`** (solides Overlay) **ist entfernt (2026-07-17)**; die zwischenzeitliche 520-ms-Sonderdauer für Mobile ist ebenfalls weg — sie kurierte das Symptom nicht und verstärkte den sichtbaren Nachhol-Sprung sogar (fester Stall = größerer Anteil an kurzer Timeline). Fallback (reduced-motion / kein origin / keine VT-API) = Token-Morph (`withColorMorph`, wie der Moduswechsel).
- **Generierung ist navigationsfest**: Der SSE-Stream lebt im globalen Store `src/state/generation.ts` (außerhalb React, `useSyncExternalStore`) — NIE in Komponenten-State zurückverlagern oder beim Unmount aborten. Streaming-UX = `ConjureStage` (magischer Kessel/Shaker, event-getriebene Phasen) + globale `GenerationPill` (schwebt über der Nav, führt zur laufenden/fertigen Generierung zurück).
- **CSP beachtet**: `script-src 'self'` — KEINE Inline-Scripts in index.html (Theme-Init liegt extern in `public/theme-init.js`).
- **Tests vor Deploy**: `pytest` + `npm test` müssen grün sein; `deploy.sh` erzwingt das. **Stand 2026-07-27: 232 Backend (94 % Statements) + 115 Frontend + 8 Playwright-E2E.** Der Frontend-API-Client hat eine eigene Suite (`src/lib/api.test.ts`, happy-dom): CSRF-Regel (Header nur bei state-changing Calls), Fehler-Mapping inkl. Nicht-JSON-Body, 204→undefined, und die **Export-Download-Mechanik** (Blob → `<a download>` → Klick → `revokeObjectURL`, Anchor darf nicht im DOM zurückbleiben) — sonst prüft das niemand.
- Touch-Targets ≥ 48 px. Lighthouse-Stand (Prod, 2026-07-11): **99/100/100/100** — bei UI-Änderungen nicht darunter fallen (CLS-Falle: Footer/Lazy-Content, siehe App.css `min-height`). Footer überall: `© 2026 Martin Pfeffer | celox.io | vN`. Die **Version** wird zur Build-Zeit aus der SW-Cache-Version (`public/sw.js` `zauberkoch-vN`) via Vite-`define __APP_VERSION__` injiziert (`vite.config.ts`) und im Footer als `.shell__version` gezeigt — **eine Quelle** (SW-Bump ⇒ Footer aktualisiert automatisch), kein zweiter Ort zu pflegen.

## Env-Variablen

Alle in `.env.example` dokumentiert. Secrets lokal in `backend/.env`, auf dem VPS in `/opt/zauberkoch-api/.env` (mode 640) — nie committen. Google-OAuth = **eigener Client** für Zauberkoch (Redirect-URIs registriert, docs/GOOGLE-OAUTH.md).

## Deploy-Kurzform (Details: docs/DEPLOY.md)

```bash
./deploy/deploy.sh                    # kompletter Deploy
ssh root@69.62.121.168 'systemctl status zauberkoch-api'
ssh root@69.62.121.168 'journalctl -u zauberkoch-api -f'
```

nginx-vHost: `/` → `/var/www/zauberkoch.de/` (SPA-Fallback), `/api/` → 127.0.0.1:8742, **SSE-Endpoint mit `proxy_buffering off`** + langen Timeouts. `www` → 301 Apex. Backup: `zauberkoch-backup.timer` nächtlich → `/var/backups/zauberkoch/`. Infrastruktur-Gesamtdoku: `/Users/martin/CLAUDE.md`.

## M3-Expressive-Kanon

Das Frontend folgt **`.claude/rules/frontend-m3e.md`** (kondensierter M3E-Kanon + Projektentscheidungen: Scheme Expressive, Seeds, benannte Hero-Momente). Tokens (`src/styles/tokens.css`, `src/motion/tokens.ts`) sind Single Source of Truth — keine Magic Numbers in Komponenten. Typo hat ein **Emphasized-Set** (`--type-*-em` + `--track-em`, Hero-Titel/Conjure-Text); gefüllte/tonale Buttons haben einen **Press-Shape-Morph** (`--shape-press`, dokumentierte border-radius-Ausnahme in motion.md).

## KI-Framework-Pflege

Dieses Repo folgt der vollen Claude-Code-Struktur. Bei jeder relevanten Änderung prüfen, ob Updates nötig sind: `CLAUDE.md` (Stack/Befehle/SW-Cache-Version), `.claude/rules/*` (Globs & Regeln), `.claude/skills/*` (Deploy-/Release-Checklisten), `.env.example` (neue Variablen), `docs/*`.
